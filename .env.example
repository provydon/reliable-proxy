# Reliable Proxy Environment Configuration

# Default target API URL (optional)
# When set, requests without a target_api_url header will be sent to this URL
TARGET_API_URL=https://api.example.com

# Server port (defaults to 8080 if not set)
# PORT=8080 